apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-alpha-sa
  namespace: assessment
  # annotations:
  #   # For Workload Identity (Phase 2 integration)
  #   iam.gke.io/gcp-service-account: app-alpha-sa@<YOUR_PROJECT_ID>.iam.gserviceaccount.com
